
var Contact = ((Contact) => {
  "use strict"

  // ----------------------------------------------------------------------
  // Init
  // ----------------------------------------------------------------------

  Contact.init = function() {
    Contact.addContainerAnimation()
    Contact.initModal()
  }


  return Contact
})(Contact || (Contact = {}))
